public class arraymodify {
    public static void main(String[] args) {
        int[] arr={1,2,3,4};
        change(arr);
        System.out.print("array is :");
        for(int i=0;i<4;i++){
            System.out.print(arr[i]);
            System.out.print(" ");
        }
    }
        nums[0]=99;  //if u make change to the object(values
       //  via ref 
        //variable i.e.nums[] then original variable also
        // get change
    }
}
